/** 행 퇴장 애니메이션 지속 시간(ms) */
export const EXIT_DURATION_MS = 300;

/** 행 퇴장 시 수평 이동 거리(px) */
export const EXIT_TRANSLATE_X = 60;

/** 모달 진입/퇴장 애니메이션 지속 시간(ms) */
export const MODAL_DURATION_MS = 200;

/** 검색 디바운스 지연 시간(ms) */
export const SEARCH_DEBOUNCE_MS = 600;
